# 🚀 ClaudeOps Smart Terminal インストール & セットアップガイド

## 📋 必要環境 | Requirements

### サーバー側 (Server Requirements)
- **OS**: macOS 12.0+ / Ubuntu 20.04+ / Windows WSL2
- **Go**: 1.19以上
- **Python**: 3.8以上
- **Node.js**: 16.0以上 (Web UI用)
- **RAM**: 最低2GB、推奨4GB以上
- **ストレージ**: 最低5GB空き容量

### モバイルアプリ側 (Mobile App Requirements)
- **iOS**: 14.0以上
- **Expo Go**: App Storeからインストール
- **ネットワーク**: Wi-Fi または モバイルデータ接続

## 🛠️ サーバーセットアップ | Server Setup

### 1. リポジトリクローン
```bash
git clone https://github.com/your-repo/ClaudeOpsSmartTerminal.git
cd ClaudeOpsSmartTerminal/server
```

### 2. 依存関係のインストール
```bash
# Go依存関係
go mod download

# Python依存関係 (W&B統合用)
pip install wandb matplotlib numpy pandas streamlit flask fastapi

# Node.js依存関係 (Web UI用)
npm install
```

### 3. Weights & Biases設定
```bash
# W&B APIキーでログイン
wandb login

# 提供されたAPIキーを入力
# 3c424d79b35640897bb8d970bbcdc872bdf9561a
```

### 4. サーバー起動オプション

#### 基本起動 (ポート8090)
```bash
./remoteclaude-server-matplotlib-mgmt
```

#### カスタムポート起動
```bash
./remoteclaude-server-matplotlib-mgmt --port=9000
```

#### VPN統合起動
```bash
# WireGuard VPN使用時
./remoteclaude-server-matplotlib-mgmt --port=8090 --vpn-mode
```

### 5. 起動確認
サーバーが正常に起動すると、以下が表示されます：

```
🚀 ClaudeOps Smart Terminal Server Started!
Connection URL: ws://192.168.1.100:8090/ws?key=abc123...
🔑 Session Key: abc123...
🌐 Web Interface: http://192.168.1.100:8080
📱 Scan QR code with mobile app
```

## 📱 モバイルアプリセットアップ | Mobile App Setup

### 1. Expo Goアプリのインストール
App Storeから「Expo Go」をダウンロード・インストール

### 2. プロジェクトのセットアップ
```bash
cd RemoteClaudeApp

# 依存関係インストール
npm install

# Expo開発サーバー起動
npx expo start
```

### 3. アプリの起動方法

#### 方法A: QRコードスキャン
1. サーバー起動時に表示されるQRコードをスキャン
2. または、`qr-code.png`ファイルをスキャン

#### 方法B: URL手動入力
1. アプリで「Manual URL Entry」を選択
2. 表示されたWebSocket URLを入力：
   ```
   ws://[サーバーIP]:8090/ws?key=[セッションキー]
   ```

#### 方法C: Expo Go経由
1. Expo Goアプリを起動
2. 開発サーバーのQRコードをスキャン

## 🔧 設定とカスタマイズ | Configuration

### サーバー設定ファイル
```json
{
  "port": 8090,
  "web_port": 8080,
  "max_connections": 10,
  "wandb": {
    "enabled": true,
    "project": "claude-smart-terminal",
    "entity": "your-team-name"
  },
  "security": {
    "enable_auth": true,
    "session_timeout": 3600
  }
}
```

### W&B設定の詳細
```bash
# プロジェクト初期化
wandb init --project="claude-smart-terminal" --entity="your-team"

# 設定確認
wandb status
```

## 🧪 動作テスト | Testing

### 1. 基本接続テスト
```bash
# サーバー側でテスト
curl -X GET http://localhost:8080/health

# 期待される応答: {"status": "ok", "version": "5.0.0"}
```

### 2. スマートターミナル機能テスト
アプリから以下のコマンドを実行して動作確認：

```bash
# 基本コマンドテスト
ls
pwd
cd /tmp

# Python実行テスト
python -c "print('Hello from Smart Terminal!')"

# matplotlib プレビューテスト
python -c "import matplotlib.pyplot as plt; plt.plot([1,2,3]); plt.show()"

# Web アプリテスト
python -m http.server 8000
```

### 3. W&B統合テスト
```python
import wandb

# テスト実行
run = wandb.init(project="test-integration")
wandb.log({"test_metric": 1.0})
wandb.finish()
```

## 🚨 トラブルシューティング | Troubleshooting

### よくある問題と解決方法

#### 1. 接続できない場合
```bash
# ファイアウォール設定確認
sudo ufw allow 8090
sudo ufw allow 8080

# ポート使用状況確認
lsof -i :8090
```

#### 2. W&B認証エラー
```bash
# APIキー再設定
wandb logout
wandb login
# APIキー再入力: 3c424d79b35640897bb8d970bbcdc872bdf9561a
```

#### 3. プレビューが表示されない
```bash
# Python依存関係再インストール
pip install --upgrade matplotlib pillow

# ポート競合確認
netstat -tuln | grep 8080
```

#### 4. モバイルアプリが起動しない
```bash
# Expo CLI更新
npm install -g @expo/cli

# キャッシュクリア
npx expo start --clear
```

### ログ確認方法
```bash
# サーバーログ
tail -f server.log

# エラーログ
tail -f error.log

# W&Bログ
wandb status --verbose
```

## 📚 使用例とサンプルコード | Examples

### AI開発プロジェクトの例
```python
# sample_ai_project.py
import wandb
import matplotlib.pyplot as plt
import numpy as np

# W&B初期化
wandb.init(project="my-ai-project")

# サンプルデータ生成
x = np.linspace(0, 10, 100)
y = np.sin(x) + np.random.normal(0, 0.1, 100)

# プロット作成
plt.figure(figsize=(10, 6))
plt.plot(x, y, 'b-', alpha=0.7)
plt.title('AI Data Visualization Example')
plt.xlabel('X values')
plt.ylabel('Y values')

# W&Bにログ
wandb.log({"sample_plot": wandb.Image(plt)})
plt.show()

print("🎯 AI project completed! Check W&B dashboard.")
wandb.finish()
```

### Streamlit Web アプリの例
```python
# streamlit_app.py
import streamlit as st
import pandas as pd
import numpy as np

st.title('🚀 Smart Terminal Demo App')

# サンプルデータ
data = pd.DataFrame({
    'x': range(10),
    'y': np.random.randn(10)
})

st.line_chart(data.set_index('x'))
st.write("Data generated by Smart Terminal!")
```

実行方法：
```bash
streamlit run streamlit_app.py
```

## 🎯 次のステップ | Next Steps

1. **開発環境のカスタマイズ**: 自分の開発スタイルに合わせた設定
2. **W&Bダッシュボード活用**: 開発効率の分析・改善
3. **プレビュー機能活用**: 作成したアプリ・グラフの即座確認
4. **Claude Code AI活用**: 複雑なタスクのAI支援開発

詳細なドキュメントとサポートについては、[公式Wiki](./WIKI.md)をご確認ください。

---
**🎉 ClaudeOps Smart Terminalで、AI駆動開発の新時代を体験しましょう！**